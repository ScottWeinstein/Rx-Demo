# Reactive Extension Demos

## Build Reqs

You **must** first compile via command line msbuild.

    > msbuild .\rx.msbuild

To run the twitter example, you'll need to create the authkeys.txt file and add your own twitter API authkey